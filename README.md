# 5 Minutes a Day (Build in Public)

Repo root contains:

- **`docs/`** – Product specs and design (V1-SPEC, V2-SPEC, V3-SPEC, DESIGN-DOC, FUTURE).
- **`app/`** – Next.js application (Vercel + Supabase). See `app/README.md` for run instructions.

To run the app locally: `cd app && npm run dev`.
